package com.ascuntar.estudioCertificacionSCJP.objetivo1;

public class O1_Identificadores {

	/**
	 * Un identificador no es mas que el nombre que reciben las clases, los
	 * metodos y las variables. 
	 * Deben empezar con letra(mayuscula o minuscula), $ o _ 
	 * No pueden empezar por un numero 
	 * Despues del primer caracter, los identificadores pueden contener letra, numero, $ o _ 
	 * No hay limite en el numero de caracteres que un identificador puede tener. 
	 * No pueden contener signos de puntuacion. 
	 * Los identificadores son "case sensitive"
	 * No pueden tener "palabras reservadas" como identificadores
	 *  
	 			Palabras Reservadas
	 
	  abstract	boolean 	break		byte			case		 catch
	  char		class		const		continue		default		 do
	  double	else		extends		final			finally		 float
	  for		goto		if			implements		import		 instanceof
	  int		interface	long		native			new			 package
	  private	protected	public		return			short		 static
	  strictfp	super		switch		synchronized	this		 throw
	  throws	transient	try			void			volatile	 while
	  assert	enum		

	 */

	// Identificadores Legales

	int a;
	int $c;
	int ______2_w;
	int _$;
	int este_es_un_nombre_muy_detalldo_de_un_identificador;
	


	// Identificadores Ilegales
	
	/**
	  int :b; 
	  int -d; 
	  int e#; 
	  int .f; 
	  int 7g;
	 
	 */


	
}
